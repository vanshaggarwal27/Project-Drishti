rules_version = '2';

// Firebase Storage Security Rules - COMPLETELY OPEN FOR DEVELOPMENT
service firebase.storage {
  match /b/{bucket}/o {
    // Allow all operations for development/demo
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
